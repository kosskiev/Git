# увидеть по какой ссылке подключено к репозиторию
git remote -v

# поменять ссылку по которой происходит подключение на сайт (последняя ссылка которую генерирует в репозитории git)
git remote set-url origin git@github.com:kosskiev/Git.git

# Если не добавляет репозиторий нужно сначала сделать
git pull
# после
git push origin 

# проверить ключи в папке 
ll ~/.ssh/

# сгенерировать пароли 
ssh-keygen

# скопировать publickkey
cat ~/.ssh/id_rsa.pub 
# этот ключ вставляем  на сайте git раздел settings ssh key
